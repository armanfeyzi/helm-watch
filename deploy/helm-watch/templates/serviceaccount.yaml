{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "helm-watch.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "helm-watch.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
