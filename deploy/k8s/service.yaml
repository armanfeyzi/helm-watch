apiVersion: v1
kind: Service
metadata:
  name: helm-watch
  namespace: helm-watch
  labels:
    app.kubernetes.io/name: helm-watch
spec:
  selector:
    app.kubernetes.io/name: helm-watch
  ports:
    - name: http
      port: 8080
      targetPort: http
